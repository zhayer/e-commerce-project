import { Checkout } from "../../types";
/**
 * Add to cart/remove from cart, cart updated events.
 */
export declare const handleCheckoutLineItemChange: (checkout: Checkout, oldCheckout: Checkout) => void;
